#include <terminal/media/model/AbstractMediaResource.hpp>

namespace terminal {
  namespace media {
    namespace model {
      AbstractMediaResource::AbstractMediaResource(terminal::media::api::string const &) {

      }
    }
  }
}
